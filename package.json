{
  "name": "openheard",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "install:all": "npm ci --prefix api && npm ci --prefix daemon && npm ci --prefix web && npm ci --prefix public",
    "typecheck": "npm run typecheck --prefix api && npm run typecheck --prefix daemon && npx --prefix web tsc -b web",
    "test": "npm test --prefix api && npm test --prefix daemon && npm test --prefix web",
    "lint": "npm run lint --prefix web",
    "build": "npm run build --prefix web && npm run build --prefix public",
    "verify": "npm run typecheck && npm run test && npm run lint && npm run build"
  }
}
